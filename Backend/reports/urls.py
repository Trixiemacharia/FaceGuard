# zones/urls.py
from django.urls import path
from . import views   # adjust imports to match your actual views

urlpatterns = [
    # Add your zone URL patterns here, e.g.:
    # path('zones/', views.ZoneListView.as_view(), name='zone-list'),
    # path('zones/<int:pk>/', views.ZoneDetailView.as_view(), name='zone-detail'),
]